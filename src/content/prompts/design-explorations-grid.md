---
title: "Design Explorations Grid"
description: "Generate multiple distinctly different design approaches in a single HTML file for side-by-side comparison."
category: "Design"
variables:
  - name: "feature"
    label: "Feature / Screen"
    placeholder: "e.g. onboarding screen, settings page, empty state..."
  - name: "count"
    label: "Number of approaches"
    placeholder: "e.g. 6"
---

I'm not sure what direction to take the {{feature}}. Generate {{count}} distinctly different approaches — vary layout, tone, and density — and lay them out as a single HTML file in a grid so I can compare them side by side. Label each with the tradeoff it's making.
